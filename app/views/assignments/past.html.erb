<p id="notice"><%= notice %></p>
<table>
    <tbody>
      <tr>
         <td> <%= link_to ' <- Back to All Assignments ', assignments_path %> </td>
         <td> <%= link_to "List of All Current Assignments", assignment_current_path %> </td>
      </tr>
    </tbody>
</table>
<h1>Past Assignments</h1>

<table>
  <thead>
    <tr>
      <th>Store</th>
      <th>Employee</th>
      <th>Start date</th>
      <th>End date</th>
      <th>Pay level</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @assignment_past.each do |assignment| %>
      <tr>
        <td><%= assignment.store_id %></td>
        <td><%= assignment.employee_id %></td>
        <td><%= assignment.start_date %></td>
        <td><%= assignment.end_date %></td>
        <td><%= assignment.pay_level %></td>
        <td><%= link_to 'Show', assignment %></td>
        <%  if valid_employee!=nil &&  (valid_employee.role=="admin") %>

        <td><%= link_to 'Edit', edit_store_path(store) %></td>
        <td><%= link_to 'Destroy', store, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            <% end %>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%  if valid_employee!=nil &&  (valid_employee.role=="admin") %>

<%= link_to 'Click to Create New Store', new_store_path %>
<% end %>