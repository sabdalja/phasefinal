<p id="notice"><%= notice %></p>

<table>
    <tbody>
      <tr>
         <td> <%= link_to ' <- Back to All Employees ', employees_path %> </td>
         <td> <%= link_to 'Inactive Employees | ', employee_inactive_path %> </td>
         <td><%= link_to 'Active Employees | ', employee_active_path %> </td>   
         <td> <%= link_to 'Employees Under 18 | ', employee_younger_than_18_path%></td>
         <td> <%= link_to 'Employees 18 & Above | ', employee_is_18_or_older_path%></td>
         <td> <%= link_to 'Regulars | ', employee_regulars_path%></td>
         <td> <%= link_to 'Managers | ', employee_managers_path%></td>
      </tr>
    </tbody>
</table>
<h1>Administrators</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Ssn</th>
      <th>Date of birth</th>
      <th>Phone</th>
      <th>Role</th>
      <th>Active</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employee_admins.each do |employee| %>
      <tr>
        <td><%= employee.first_name %></td>
        <td><%= employee.last_name %></td>
        <td><%= employee.ssn %></td>
        <td><%= employee.date_of_birth %></td>
        <td><%= employee.phone %></td>
        <td><%= employee.role %></td>
        <td><%= employee.active %></td>
        <td><%= link_to 'Show', employee %></td>
        <%  if valid_employee!=nil &&  (valid_employee.role=="admin") %>

        <td><%= link_to 'Edit', edit_employee_path(employee) %></td>
        <td><%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%  if valid_employee!=nil &&  (valid_employee.role=="admin") %>

<%= link_to 'Click to Create New Employee', new_employee_path %>
<%end%>